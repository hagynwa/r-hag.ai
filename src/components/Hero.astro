<section class="hero">
  <div class="hero-bg-shapes">
    <div class="hero-shape hero-shape-1"></div>
    <div class="hero-shape hero-shape-2"></div>
    <div class="hero-shape hero-shape-3"></div>
  </div>
  <div class="hero-inner">
    <div class="hero-text">
      <h1>
        <span data-en="Building AI that" data-he="בונה AI">Building AI that</span><br>
        <span class="gradient-text" data-en="replaces bureaucracy with clarity." data-he="שמחליפים ביורוקרטיה בבהירות">replaces bureaucracy with clarity.</span>
      </h1>
      <p class="hero-subtitle" data-en="<strong>Leading AI-based development projects end-to-end</strong> at Maccabi Health Services. MBA Lecturer, community builder, and maker of 10+ AI products that actually ship." data-he="<strong>מוביל פרויקטי פיתוח מבוססי AI מקצה לקצה</strong> בשירותי בריאות מכבי. מרצה MBA, מוביל קהילה, ויוצר של 10+ מוצרי AI שבאמת עובדים.">
        <strong>Leading AI-based development projects end-to-end</strong> at Maccabi Health Services. MBA Lecturer, community builder, and maker of 10+ AI products that actually ship.
      </p>
      <p class="hero-intro" data-en="I believe in &ldquo;less but better.&rdquo; From ambient listening in healthcare to AI agents that handle paperwork &mdash; I build tools that solve real human problems." data-he="המוטו שלי: &ldquo;פחות, אבל טוב יותר&rdquo;. מ-ambient listening בעולם הבריאות ועד סוכני AI שמטפלים בביורוקרטיה &mdash; אני בונה כלים שהופכים מורכבות לפשטות.">
        I believe in &ldquo;less but better.&rdquo; From ambient listening in healthcare to AI agents that handle paperwork &mdash; I build tools that solve real human problems.
      </p>
      <div class="hero-cta">
        <a href="#projects" class="btn btn-primary" data-en="See My Projects" data-he="הפרויקטים שלי">See My Projects</a>
        <a href="#contact" class="btn btn-outline" data-en="Get in Touch" data-he="צור קשר">Get in Touch</a>
        <a href="/hagai-cv.pdf" target="_blank" rel="noopener" class="btn btn-outline" data-en="Download CV" data-he="הורד קורות חיים">Download CV</a>
      </div>
    </div>
    <div class="hero-photo">
      <div class="photo-container">
        <div class="photo-ring"></div>
        <div class="photo-ring photo-ring-2"></div>
        <div class="photo-frame">
          <img src="/profile.jpeg" alt="Hagai Rettig">
        </div>
        <div class="floating-badge">
          <div class="badge-icon badge-teal">&#x1f3c6;</div>
          <span data-en="5 IT Awards" data-he="5 פרסי IT">5 IT Awards</span>
        </div>
        <div class="floating-badge">
          <div class="badge-icon badge-warm">&#x1f680;</div>
          <span data-en="10+ AI Products" data-he="10+ מוצרי AI">10+ AI Products</span>
        </div>
        <div class="floating-badge">
          <div class="badge-icon badge-purple">&#x1f393;</div>
          <span data-en="M.Sc. Technion" data-he="M.Sc. טכניון">M.Sc. Technion</span>
        </div>
        <div class="floating-badge">
          <div class="badge-icon badge-teal">&#x1f3eb;</div>
          <span data-en="MBA Lecturer" data-he="מרצה MBA">MBA Lecturer</span>
        </div>
        <div class="floating-badge">
          <div class="badge-icon badge-warm">&#x1f3e5;</div>
          <span data-en="HealthTech" data-he="הלת'טק">HealthTech</span>
        </div>
        <div class="floating-badge">
          <div class="badge-icon badge-purple">&#x1f396;&#xfe0f;</div>
          <span data-en="Givati Combat" data-he="לוחם גבעתי">Givati Combat</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
    background: var(--bg);
  }

  :global([data-theme="dark"]) .hero {
    background: radial-gradient(circle at center, #1e293b 0%, #0f172a 100%);
  }

  .hero-bg-shapes {
    position: absolute; inset: 0; overflow: hidden; pointer-events: none;
  }

  .hero-shape {
    position: absolute; border-radius: 50%; filter: blur(80px); opacity: 0.4;
    animation: float 20s ease-in-out infinite;
  }

  .hero-shape-1 { width: 600px; height: 600px; background: radial-gradient(circle, var(--accent-light), transparent 70%); top: -200px; right: -100px; animation-delay: 0s; }
  .hero-shape-2 { width: 400px; height: 400px; background: radial-gradient(circle, var(--warm-light), transparent 70%); bottom: -100px; left: -50px; animation-delay: -7s; }
  .hero-shape-3 { width: 300px; height: 300px; background: radial-gradient(circle, rgba(139,92,246,0.15), transparent 70%); top: 40%; left: 30%; animation-delay: -14s; }

  @keyframes float {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(30px, -30px) scale(1.05); }
    66% { transform: translate(-20px, 20px) scale(0.95); }
  }

  .hero-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 8rem 2rem 4rem;
    display: grid;
    grid-template-columns: 1fr 420px;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .hero-text h1 .gradient-text {
    background: linear-gradient(135deg, var(--accent) 0%, #0ea5e9 50%, var(--purple) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    background-size: 200% auto;
    animation: gradient-shift 4s ease-in-out infinite;
  }

  @keyframes gradient-shift {
    0%, 100% { background-position: 0% center; }
    50% { background-position: 100% center; }
  }

  .hero-subtitle { font-size: 1.15rem; color: var(--text-secondary); line-height: 1.75; margin-bottom: 1rem; }
  .hero-subtitle strong { color: var(--text-primary); font-weight: 600; }
  .hero-intro { font-size: 1rem; color: var(--text-muted); margin-bottom: 2.5rem; line-height: 1.8; }

  .hero-cta { display: flex; gap: 1rem; margin-bottom: 3.5rem; flex-wrap: wrap; }

  .btn {
    display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.8rem 1.8rem;
    font-family: var(--font-body); font-size: 0.95rem; font-weight: 600; border-radius: 10px;
    text-decoration: none; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    cursor: pointer; border: 2px solid transparent; position: relative; overflow: hidden;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--accent), var(--accent-hover));
    color: #fff; border-color: var(--accent); box-shadow: 0 4px 15px var(--accent-glow);
  }

  .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 25px var(--accent-glow); color: #fff; }

  .btn-outline {
    background: var(--bg-white);
    color: var(--accent); border-color: var(--accent);
  }
  :global([data-theme="dark"]) .btn-outline {
    background: var(--bg-card);
  }

  .btn-outline:hover { background: var(--accent-light); transform: translateY(-2px); }

  .hero-photo { display: flex; justify-content: center; align-items: center; }

  .photo-container { position: relative; width: 360px; height: 360px; }

  .photo-ring {
    position: absolute; inset: -12px; border-radius: 50%;
    border: 2px dashed var(--accent); opacity: 0.3;
    animation: spin-slow 30s linear infinite;
  }
  .photo-ring-2 { inset: -28px; border-color: var(--purple); opacity: 0.15; animation-direction: reverse; animation-duration: 45s; }

  @keyframes spin-slow { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

  .photo-frame {
    width: 100%; height: 100%; border-radius: 50%;
    background: linear-gradient(135deg, var(--accent-light) 0%, #ddd6fe 50%, var(--warm-light) 100%);
    border: 4px solid var(--bg-white);
    box-shadow: var(--shadow-lg), 0 0 60px var(--accent-glow);
    display: flex; align-items: center; justify-content: center; overflow: hidden; position: relative;
    transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .photo-frame:hover { transform: scale(1.03); }
  .photo-frame img { width: 100%; height: 100%; object-fit: cover; position: absolute; inset: 0; }

  .floating-badge {
    position: absolute; background: var(--bg-white); border: 1px solid var(--border);
    border-radius: 12px; padding: 0.5rem 0.9rem; box-shadow: var(--shadow-md);
    display: flex; align-items: center; gap: 0.5rem; font-size: 0.78rem; font-weight: 600;
    color: var(--text-primary); animation: badge-float 6s ease-in-out infinite; z-index: 2; white-space: nowrap;
  }
  
  :global([data-theme="dark"]) .floating-badge {
    background: var(--bg-dark-card);
  }

  .floating-badge .badge-icon {
    width: 28px; height: 28px; border-radius: 8px;
    display: flex; align-items: center; justify-content: center; font-size: 0.9rem;
  }

  .badge-teal { background: var(--accent-light); }
  .badge-warm { background: var(--warm-light); }
  .badge-purple { background: #ede9fe; color: #000; }
  :global([data-theme="dark"]) .badge-purple { background: rgba(139, 92, 246, 0.2); color: #fff; }

  .floating-badge:nth-child(2) { top: 5%; right: -20px; animation-delay: 0s; }
  .floating-badge:nth-child(3) { bottom: 18%; left: -50px; animation-delay: -1.5s; }
  .floating-badge:nth-child(4) { top: 35%; right: -55px; animation-delay: -3s; }
  .floating-badge:nth-child(5) { bottom: 45%; left: -45px; animation-delay: -4.5s; }
  .floating-badge:nth-child(6) { top: 65%; right: -25px; animation-delay: -2s; }
  .floating-badge:nth-child(7) { top: -5%; left: 30%; animation-delay: -5s; }

  @keyframes badge-float { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-8px); } }

  @media (max-width: 1024px) {
    .hero-inner { grid-template-columns: 1fr; text-align: center; padding-top: 7rem; gap: 2.5rem; }
    .hero-subtitle, .hero-intro { max-width: none; }
    .hero-cta { justify-content: center; }
    .hero-photo { order: -1; }
    .photo-container { width: 280px; height: 280px; }
    .floating-badge { display: none !important; }
  }
  @media (max-width: 480px) {
    .hero-inner { padding: 6rem 1rem 3rem; }
    .photo-container { width: 200px; height: 200px; }
  }
</style>
