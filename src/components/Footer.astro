<footer>
  <div class="footer-inner">
    <div class="tagline" data-en="Built with signal, not noise." data-he="סיגנל, לא רעש.">Built with signal, not noise.</div>
    <div class="copy">r-hag.ai &copy; {new Date().getFullYear()} Hagai Rettig</div>
    <ul class="footer-links">
      <li><a href="https://linkedin.com/in/hagai-rettig" target="_blank" rel="noopener">LinkedIn</a></li>
      <li><a href="https://github.com/hagynwa" target="_blank" rel="noopener">GitHub</a></li>
      <li><a href="mailto:hagai.rettig@gmail.com">Email</a></li>
    </ul>
  </div>
</footer>

<style>
  footer {
    background: var(--bg-dark);
    color: rgba(255,255,255,0.5);
    text-align: center;
    padding: 3rem 2rem;
    position: relative;
    overflow: hidden;
  }

  footer::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: linear-gradient(90deg, var(--accent), var(--purple), var(--warm), var(--pink), var(--accent));
    background-size: 200% auto;
    animation: gradient-shift 4s linear infinite;
  }

  footer .tagline {
    font-family: var(--font-heading);
    font-size: 0.95rem;
    color: rgba(255,255,255,0.75);
    margin-bottom: 0.5rem;
  }

  footer .copy { font-size: 0.8rem; }

  footer .footer-links {
    list-style: none;
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-top: 1.2rem;
  }

  footer .footer-links a {
    color: rgba(255,255,255,0.4);
    text-decoration: none;
    font-size: 0.85rem;
    transition: color 0.2s;
  }

  footer .footer-links a:hover { color: var(--accent); }
</style>
